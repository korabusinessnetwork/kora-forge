# {{PROJETO}}, variáveis de ambiente. Copie para .env.local e preencha.
#
# Este arquivo é versionado e leva apenas os NOMES das variáveis, jamais valores.
# Chave secreta nunca entra aqui, nunca entra no front, e nunca entra no repositório.
#
# Deploy: {{DEPLOY}}

# URL base da API. Vazio usa /api na mesma origem.
VITE_API_URL=
